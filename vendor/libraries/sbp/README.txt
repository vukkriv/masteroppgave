To update this library, do the following:

1. Fetch the release from https://github.com/swift-nav/libsbp
  - (Either use the generated files, or generate manually.)
2. Copy all header files, including sbp.h, edc.h, common.h
3. Copy the source files sbp.c, edc.c
